
// we had the new comment at the top, just after the form and hide the form

$('.forum_subject_<%= @forum_subject.id%>').replaceWith("<%= j render partial: 'forum_subjects/forum_subject', locals: {forum_subject: @forum_subject}  %>");

// close the modal
// REMOVED no more use of the modal
// $('#app_dialog').modal_success();

// first we need to hide before to show
var target = $('.forum_subject_<%= @forum_subject.id%>');
$('html,body').animate({
    scrollTop: target.offset().top-100
}, 1000);








