$("#follow_form_<%= @followed.id%>").html("<%= escape_javascript(link_to("Follow", follow_peer_path(followed_id: @followed.id), method: :post, remote: true )) %>")

$('div#followed-list > div#customer_presentation_<%= @followed.id%>').fadeOut("normal", function() {
    $(this).remove();
});
