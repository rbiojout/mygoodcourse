$("#follow_form_<%= @followed.id%>").html("<%= escape_javascript(link_to("Unfollow", unfollow_peer_path(followed_id: @followed.id), method: :delete, remote: true )) %>")

// we add the new followed from the list
$('#followed-list').prepend("<%= j render partial: 'customers/customer', locals: {customer: @followed}  %>");
