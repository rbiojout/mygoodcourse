<%= link_to(follow_peer_path(followed_id: followed.id, locale: locale), class: 'btn btn-default btn-sm', :method => :post, remote: true ) do %>

    <i class="fa fa-heart-o red"> </i> <%= t('activerecord.attributes.peers.follow') %>
<% end %>

