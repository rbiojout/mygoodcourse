
// we had the new comment at the top, just after the form and hide the form
$('#forum-answer-form').html("");
$('#forum-answer-form').before("<%= j render partial: 'forum_answers/forum_answer', locals: {forum_answer: @forum_answer}  %>");


// close the modal
// REMOVED no more use of the modal
// $('#app_dialog').modal_success();

// first we need to hide before to show
var target = $('#forum-answer-form');
$('html,body').animate({
    scrollTop: target.offset().top+100
}, 1000);










