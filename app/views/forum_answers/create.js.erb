
// we had the new comment at the top, just after the form and hide the form
$('#forum_answer-form').html("");
$('#forum_answer-form').before("<%= j render partial: 'forum_answers/forum_answer', locals: {forum_answer: @forum_answer}  %>");


// activate the editor
$('form[data-model="forum_answer"] .summernote').summernote({
    lang: I18n_locale,
    // adjust initial height for the Summernote Editor
    height: '200px'
})

// close the modal
// REMOVED no more use of the modal
// $('#app_dialog').modal_success();

// first we need to hide before to show
var target = $('#forum_answer-form');
$('html,body').animate({
    scrollTop: target.offset().top + 100
}, 1000);










