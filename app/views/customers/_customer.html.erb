<% withcols ||= 'true' %>
<% customer.locality =nil if customer.locality.to_s.size ==0 %>
<div class="<%= 'col-xs-6 col-sm-3' if withcols == 'true' %> customer_presentation" id="customer_presentation_<%= customer.id%>">
  <div class="thumbnail">
  <img class="media-object img-circle customer-picture"
       data-customer="<%=customer.id %>"
       data-locality=  "<%= t('activerecord.attributes.customer.located', :locality => customer.locality||'Secret') %>"
       data-created="<%=t('activerecord.attributes.customer.since', :created => l(customer.created_at, :format => :short) )%>" alt="" src="<%= customer.picture %>" style="width:80px;">
    <div class="caption">
      <h5 class="media-heading two_lines"><%= customer.first_name %> <%= customer.name %></h5>

    <% if customer_signed_in? %>
      <% if current_customer.id != customer.id %>
      <h5>
      <div class="follow_form" id="follow_form_<%= customer.id%>">
        <% if current_customer.following?(customer) %>
            <%= link_to "Unfollow", unfollow_peer_path(followed_id: customer.id), method: :delete, remote: true %>
        <% else %>
            <%= link_to "Follow", follow_peer_path(followed_id: customer.id), method: :post, remote: true  %>
        <% end %>
      </div>
      </h5>

      <% else %>
          <h5>.</h5>
      <% end %>

    <% else %>
  <% end %>

  <div class="two_lines">
  since <%= content_tag('small', nice_date(customer.created_at), :class => 'small') %>
  </div>
  <div class="ratings">
    <% unless customer.score_comments.nil? %>
        <%= render "comments/show_stars", :score => customer.score_comments, :simple => true %>
        <p><%= customer.nb_comments.to_s + ' reviews' unless customer.nb_comments.nil? %></p>

    <% else %>
        <%= render "comments/show_stars", :score => 0.0, :simple => true %>
        <p>Not rated</p>
    <% end %>
  </div>
      </div>
    </div>
</div>